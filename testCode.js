/**var game = new subGame(0, document.getElementById('f11'));
*var game = new subGame(0, document.getElementById('f12'));
*var game = new subGame(0, document.getElementById('f13'));
*var game = new subGame(0, document.getElementById('f21'));
*var game = new subGame(0, document.getElementById('f22'));
*var game = new subGame(0, document.getElementById('f23'));
*var game = new subGame(0, document.getElementById('f31'));
*var game = new subGame(0, document.getElementById('f32'));
*var game = new subGame(0, document.getElementById('f33'));
*/
//game.movePlayed(1,0);